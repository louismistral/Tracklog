# Fonctionnalités de Tracklog

L'inventaire de ce que l'app sait faire, et pourquoi chaque chose est faite
ainsi. Ouvrir ce fichier **avant de proposer une fonctionnalité** (pour voir si
elle existe déjà, sous un autre nom) et **avant d'en modifier une** (pour
retrouver la raison de sa forme actuelle).

## Fonctionnalités


- **Trackers configurables** — 6 types de données + un type calculé (master), fréquence quotidienne ou multi-entrées, agrégat, unité, couleur, fenêtre d'activité, archivage réversible. Chaque carte du Log porte son propre engrenage vers les réglages (`TrackerModal`) — pas d'onglet dédié, voir « Écrans ».
- **Tracker synchronisé** — un tracker de type nombre peut être rempli par un service extérieur au lieu de la main (`TrackerModal` → **Source**). Etsy pour commencer : le bénéfice, les ventes ou le nombre de commandes d'un jour s'écrivent dans la journée correspondante. La connexion se fait en un bouton (OAuth avec PKCE, ouvert dans un onglet à part), la relecture est automatique au chargement quand la dernière date de plus d'une demi-heure, et deux boutons restent : **Synchroniser** (les jours récents) et **Tout relire**. Les entrées produites sont des entrées ordinaires : corrigeables à la main, et gardées si on débranche la source.
- **Case joker** — exclure une journée entière des calculs sans la compter comme un échec. Sur la carte, active un bouton rond (icône « annuler ») et remplace la ligne de saisie par un tiret « — · ce jour ne compte pas » plutôt que de laisser un champ vide inviter à noter quelque chose.
- **Masters (indices composites)** — moyenne normalisée 0–100 de plusieurs trackers, avec bande de lecture (Master Strip) et carte graphe dédiée.
- **Graphes par tracker** avec axes auto-arrondis (`niceDomain`/`niceStep`), pontage en pointillés des trous de données, mode cumulatif optionnel.
- **Échelle verticale intelligente** — les bornes sont l'arrondi propre le plus proche des valeurs extrêmes (jamais les extrêmes bruts, jamais un zéro forcé), le pas est constant et choisi pour viser ~6 graduations, et le nombre de décimales des étiquettes se déduit du pas — deux graduations ne peuvent plus afficher le même nombre. Recalculé à chaque changement de période.
- **Échelle personnalisable** — un tracker `scale` définit son propre min/max/incrément (`TrackerModal`), plutôt qu'un simple « 1 à N » ; le curseur (Log, Historique, `EntryModal`) et l'axe du graphe (`ChartCard`) suivent.
- **Sens de l'amélioration** — un tracker `number`/`scale`/`duration` déclare si monter, descendre, ou se rapprocher d'une valeur cible est le progrès (`goodDirection`/`targetValue`, `TrackerModal`). `normalizeSeries` en tient compte pour tout composite (Master, Tendance générale) et `GridSummary` colore sa flèche de tendance en conséquence — un temps d'écran qui baisse se lit comme une amélioration, pas une chute.
- **Forme de courbe et granularité par tracker** — polyligne, courbe lissée ou bâtons, et un point = un jour / une semaine / un mois, réglables indépendamment dans la section « Vues » des paramètres du tracker (masters compris). Les bâtons partent de zéro quand l'échelle le contient, du bas du cadre sinon, et ne pontent pas les jours vides — un trait entre deux points affirme un passage que les bâtons ne prétendent pas connaître.
- **Tooltip flottant sur les graphes** — survol souris (immédiat) ou appui maintenu au doigt affiche la valeur du jour pointé, avec un bouton rond pour ouvrir ce jour dans l'Historique (édition directe) et un bouton rond pour fermer. Une fois ouvert, le doigt balaie librement la courbe.
- **Densité des cartes de graphe** — un curseur « − / + » règle de 1 à 3 cartes par ligne. Monter d'un cran ne fait pas que rétrécir : la carte se déleste de ses statistiques secondaires puis de ses graduations, et finit en sparkline avec sa seule valeur du jour. Bridé à 2 colonnes sous 880 px et à 1 sur téléphone — c'est donc sur grand écran qu'il change vraiment quelque chose, ce que dit sa bulle. Au-delà de trois par ligne, une carte est plus étroite que son propre axe.
- **Vue Master overlay** et **Tendance générale** — comparer plusieurs trackers normalisés sur le même graphe, ou leur moyenne lissée sur 7 jours.
- **Heatmap calendrier** par tracker (`CalendarCard`) et **grille de KPI** (`GridSummary`, avec variation vs période précédente).
- **Historique éditable** — calendrier mensuel, ouvrir/éditer n'importe quel jour passé.
- **Chronos** — plusieurs chronomètres, mode Solo/Multi, fenêtre flottante (Document Picture-in-Picture), conversion directe en entrée sur un tracker durée. Synchronisés sur le compte (voir « Architecture technique ») et réordonnables comme les cartes du Log.
- **Réordonnancement par glisser-déposer** — trackers, cartes du jour, master strips, chronos, **sections du Jour** ; un ordre global unique par liste (par mode de groupement pour les sections), chaque vue n'affiche/réordonne qu'un sous-ensemble sans perturber le reste (`mergeSubOrder`, `mergeSectionOrder`). Au doigt il faut maintenir l'appui avant que ça s'attrape, avec une brève vibration pour le dire. **Réorganiser les sections du Jour replie tout leur contenu** le temps du glisser : il ne reste que les intitulés, et la liste entière tient sous les yeux au lieu de s'étaler sur plusieurs écrans. Replié en CSS (`.day-groups.reordering .day-group-body`), jamais démonté — une carte à moitié remplie garde son brouillon.
- **Filtre, tri et groupe** (rail) — trois boutons désormais séparés : filtrer un sous-ensemble de trackers sur Log/Vues, trier Manuel/A→Z/Récents/Type, et **grouper** le "Jour" en sections par type (le cas d'origine), par couleur, ou par fait/pas fait aujourd'hui. Chaque section (Masters, Quotidiens, Plusieurs par jour, Alimentation, ou une section couleur/fait) se réordonne à la souris ou au doigt, comme une carte. Tout est retrouvé tel quel au rechargement ; un tracker supprimé ailleurs est purgé de la sélection une fois les trackers chargés.
- **Bulles infos** — une explication a **une** forme dans toute l'app : un « i » à côté du réglage, qui déplie un cadre **dans le corps de la page**, sous ce qu'il explique, en poussant la suite vers le bas (`InfoBubble`). Pas un calque : un calque recouvre justement ce dont il parle. Le cadre porte en titre le **nom** de la chose expliquée — le mot qu'on retrouvera ailleurs dans l'app — et son texte met en gras ce qui compte. Un seul interrupteur les allume ou les éteint (Paramètres › Affichage › Bulles infos). Les paramètres écrivaient les leurs en clair sous chaque ligne ; l'interrupteur parlait alors de deux choses à la fois, et son nom ne disait ni l'une ni l'autre. Celle qui explique l'interrupteur lui-même ne se masque jamais — c'est la porte pour rallumer les autres.
- **Styles** — sélecteur à N styles (Sombre et Clair aujourd'hui), chaque choix montrant les couleurs qu'il applique. Préférence par appareil, appliquée avant le premier rendu pour ne jamais faire clignoter les mauvaises couleurs.
- **Onglets activables** — masquer n'importe quel onglet depuis les paramètres, **Log compris** : cinq lignes identiques dans la même carte ne peuvent pas se comporter différemment sans qu'une exception passe pour une panne. Rien n'est supprimé : l'onglet disparaît de la barre, les données restent, et masquer celui qu'on regarde renvoie au premier onglet encore affiché. Les paramètres, eux, ne sont pas un onglet mais l'engrenage du bout de barre — c'est de là qu'on rallume, donc tout masquer reste permis. L'analyse IA de Food **n'en fait pas partie** : ce n'est pas un onglet du haut mais une des quatre façons d'ajouter à manger, et on ne masque pas l'une sans les autres. Réglage synchronisé sur le compte.
- **Onglets réordonnables** — maintenir un onglet de la barre du haut (ou une ligne de Paramètres › Onglets) permet de le déplacer ; l'ordre suit le compte. Un onglet masqué garde son créneau et le retrouve quand on le rallume.
- **Couleur d'accent** — le même nuancier que celui d'un tracker, dans Paramètres › Thème, pour choisir la couleur des boutons, des liens et de l'état actif. Une pastille à part remet celle de Tracklog. Suit le compte, et se pose avant le premier rendu comme le thème — sinon l'app clignoterait en orange à chaque ouverture.
- **Nuancier de couleurs** — dix ronds sur une ligne : sept teintes, un gris, l'encre du thème, et le « + » qui ouvre l'éditeur à trois curseurs et pipette. Une couleur perso occupe la place du « + » et le rouvre sur elle.
- **Numéro de semaine** — affiché à côté de la date du Log et de l'Historique, activable/désactivable dans les paramètres (Affichage). Préférence par appareil.
- **Retours intégrés** — bug, idée, avis ou autre, écrits depuis les paramètres et enregistrés en base, avec le contexte technique (style, taille d'écran, navigateur) capté automatiquement.
- **Items catégorisés** — quatre origines posées explicitement (aliment simple · aliment à code · item IA · personnel), une pastille sur chaque item partout où il se montre, et la sous-catégorie qui existait déjà (groupe Ciqual, marque) affichée à côté. L'origine était déjà dans le code ; elle est maintenant dite.
- **Une page d'ajout, quatre onglets** — Recherche · Mes items · IA · Ajout rapide. Le bouton principal de Food est « Ajouter », plus « Scanner un produit » : le scan est une façon d'ajouter, pas la porte d'entrée.
- **Food : scanner de code-barres** — caméra (BarcodeDetector natif ou ZXing en repli), plusieurs passes de recadrage/rotation. Il ne fait plus que scanner : ni photo de secours ni champ à lui. Un code qui ne se lit pas se tape dans le champ à côté (barre de recherche, ou champ code-barres de l'ajout rapide), qui reconnaissent une suite de chiffres pour ce qu'elle est.
- **Recherche Open Food Facts** — plusieurs moteurs en cascade, cache et limiteur de débit (quota OFF).
- **Table Ciqual embarquée** — recherche instantanée hors-ligne des aliments sans étiquette (légumes, viandes brutes…), avec micronutriments.
- **Snapshot nutritionnel** — chaque ligne de repas figée à sa valeur du moment ; corriger un aliment plus tard ne modifie pas l'historique.
- **Interrupteur caméra** — le scanner ne demande aucun flux tant qu'il est éteint ; le choix suit le compte, si bien qu'une fois allumé il démarre seul, ici comme sur l'autre appareil. Ce qui est synchronisé est l'intention « je veux la caméra » ; l'autorisation du navigateur, elle, reste propre à chaque appareil.
- **Favoris** — une étoile range un item dans les favoris, aliment comme repas, pour retrouver en un geste ce qu'on mange tous les jours.
- **Carte d'item** — trois zones en grille : l'image à gauche quand on l'a demandée, le nom et sa sous-catégorie au milieu (avec, en dessous, la pastille d'origine suivie des trois actions : fiche d'origine, favori, corbeille ou crayon), et les chiffres à droite. La pastille d'origine a une **largeur fixe** — celle du plus long des quatre mots — pour que les trois ronds tombent au même endroit sur toutes les cartes : alignés, ils deviennent une colonne qu'on vise sans regarder. Les actions sont des `.icon-btn.xs`, à la hauteur exacte de la pastille.
- **Colonnes de macros** (`MacroStrip`) — les quatre chiffres d'un item sur **une ligne**, calories calées à gauche et les trois macros en **colonnes de largeur fixe** à droite, le tout dans un bloc de **largeur fixe** aligné sur la ligne du titre. Aucun trait dessiné : c'est l'alignement qui fait le tableau. Ni les chiffres (tabulaires) ni la légende (`kcal/100 g`, `kcal/100 ml`, `kcal/part`) ne peuvent faire varier cette largeur — sans quoi les colonnes se décaleraient d'une carte à l'autre selon l'unité de l'aliment. Les calories restent en **encre neutre** : c'est la somme des trois, pas une quatrième macro. Mêmes colonnes, en plus grand, dans les fenêtres de quantité.
- **Barre de composition** — **sous** les chiffres, sur toute la largeur du bloc — donc la même longueur sur toutes les cartes, sans quoi comparer deux barres reviendrait à comparer deux règles différentes. Elle découpe les **calories** de l'aliment en protéines, glucides et lipides (en calories, pas en grammes : un gramme de lipide en pèse 9) et les chiffres prennent alors la couleur de leur macro — les lettres P/G/L s'effacent, la couleur les nomme, et deux façons de dire la même chose sur la même ligne en font une de trop. Un poulet est presque tout rouge, des flocons presque tout bleu. Réglage de compte (Paramètres › Affichage), affichée par défaut — la carte marche avec et sans, et les colonnes ne bougent pas d'un état à l'autre.
- **Une recherche pour deux onglets** — la barre de « Recherche » et celle de « Mes items » partagent leur saisie : chercher « poulet » dehors puis passer à ses items pose la même question à une autre source. La requête réseau, elle, reste attachée à l'onglet Recherche — filtrer ses propres items n'a pas à dépenser le quota d'Open Food Facts.
- **Un item n'entre dans la bibliothèque que si on s'en sert** — un aliment du dehors était enregistré au clic : ouvrir sa fiche pour lire ses calories, puis annuler, laissait un item de plus. Il attend maintenant de côté (`pendingSave`) et n'est écrit qu'à la validation de la quantité.
- **Raccourcis de quantité** — la fenêtre « quelle quantité ? » n'en propose **aucun** au départ : « 50 g / 100 g / 200 g » étaient des devinettes, et personne ne pèse par nombres ronds. Le « + » enregistre la valeur qu'on vient de taper, la croix la retire, quatre au maximum pour qu'ils tiennent sur la ligne du champ. Rangés par unité (g, ml, portion) et suivant le compte.
- **Un champ vide dit qu'il est vide** — la quantité n'est plus pré-remplie : un chiffre déjà là fait croire que c'est la bonne valeur, et il faut l'effacer avant de taper la sienne. Seule la correction d'une ligne déjà notée rouvre avec sa valeur.
- **Un scan qui ne trouve rien mène quelque part** — code absent des bases, ou fiche référencée mais vide : la page de création s'ouvre avec le code déjà rempli et **dit ce qui vient de se passer** — ce qui a raté, et qu'une seule saisie suffit pour que le produit sorte tout seul au scan suivant, hors-ligne compris. Elle propose aussi de le rendre à Open Food Facts (créer la fiche, ou compléter celle qui est vide), en disant que c'est un geste **à part** : ça sert aux autres, ça ne remplit pas le formulaire du dessous.
- **Supprimer une ligne de journal d'un geste** — la pousser vers la droite. Le rouge est **derrière** elle et se découvre à mesure qu'elle s'écarte : on voit ce qui va arriver pendant qu'on le fait, et lâcher avant le seuil (42 % de la largeur) la remet en place. La direction du geste se tranche au bout de 8 px en comparant les deux axes — avant ça, le doigt doit encore pouvoir faire défiler la page.
- **Oublier un item** — la corbeille d'une ligne le retire de ce qui est à soi, après confirmation, comme s'il n'avait jamais servi. Les journées déjà notées ne bougent pas : chaque ligne de journal garde son instantané de valeurs, seul le lien vers la fiche disparaît.
- **Carte d'aliment** — une seule ligne d'actions : favori · modifier · supprimer, puis la source réduite à un bouton rond portant sa flèche (elle sort de l'app, elle ne pèse pas autant que ce qu'on fait dedans). La suppression est là, pas seulement au fond de la fiche d'édition.
- **Compteurs de Food** — les calories prennent toute la largeur, les trois macros se partagent la ligne suivante, et chaque objectif se lit « actuel / cible ». L'engrenage n'est que sur la carte calories : il ouvre les objectifs des quatre. Une couleur par macro, portée par le nom **et** par la jauge : vert les calories, rouge les protéines, bleu les glucides, jaune les lipides. Dépasser sa cible calorique de plus de **2,5 %** fait passer le chiffre, le nom et la barre au rouge — en deçà, c'est la précision de l'estimation, pas un écart. Rien de tel sur les macros : dépasser ses protéines n'est pas une mauvaise nouvelle.
- **Repas enregistrés (presets)** — un ensemble d'ingrédients ajouté d'un coup, une ligne de journal par ingrédient (chacune reste corrigeable seule), avec une recette facultative en liste d'étapes. L'éditeur est une page à trois cadres — titre, ingrédients, recette — et chaque liste s'allonge par une ligne pointillée pleine largeur.
- **Composer un repas depuis n'importe quelle source** — « ＋ Ajouter un ingrédient » rouvre la page d'ajout en mode « choisir » : recherche, mes items, IA, à la main. Un repas peut donc contenir un produit scanné, un aliment Ciqual, une analyse IA, ou tous les ingrédients d'un autre repas versés d'un coup.
- **Ingrédients réordonnables** — la même poignée que les étapes juste en dessous, et le même geste. L'ordre est celui de la liste enregistrée : rien à stocker de plus, une recette se relit dans l'ordre où on l'a rangée.
- **Étapes de recette** — le texte s'enroule sur plusieurs lignes, le rond numéroté se coche comme une todo (le texte passe en gris) et s'attrape en appui maintenu pour réordonner ; les numéros se refont tout seuls. Cocher n'est gardé que si on enregistre le repas — cocher en cuisinant puis fermer ne laisse aucune trace.
- **Minuteur d'étape** — une pastille facultative à côté de chaque étape (« 10 min »). Avec une durée, le rond numéroté change de métier : le premier tap lance le décompte (la pastille l'affiche, le rond bat), le second l'arrête en validant l'étape. Sans durée, il coche comme avant. La durée suit le repas ; le décompte, lui, ne vit que pendant qu'on cuisine.
- **Un aliment ne se corrige pas** — ni dans la bibliothèque, ni dans un repas. Ce qui vient de Ciqual ou d'Open Food Facts est traité comme la fiche de l'aliment, pas comme un brouillon : dans une recette, seul le **poids** d'un ingrédient se règle. Pour autre chose, l'onglet Créer fabrique l'aliment qu'on veut vraiment, et la corbeille oublie celui dont on ne veut plus.
- **Analyse IA d'un repas** — décrire un plat en texte, joindre une photo, ou les deux : envoyés dans la même requête, Claude le décompose en ingrédients pesés avec leurs valeurs pour 100 g, sa marge d'erreur et sa cause. La photo, quand elle est là, prime sur le texte pour la composition visible ; le texte sert à préciser ce qu'elle ne montre pas. Le résultat est entièrement éditable avant d'être versé au journal.
- **Normal / Approfondi** — une bascule au-dessus du bouton Analyser. En approfondi, le modèle cherche sur le web de vraies références (nommer l'établissement est ce qui lui donne quelque chose à chercher) et remplit les micronutriments, qui se cumulent alors dans le détail du jour comme ceux d'une étiquette. Ce qu'il a lu est affiché en clair sous le résultat.
- **En faire un item ?** — verser une analyse dans la journée et la garder dans Mes items sont deux gestes : la page pose la question (`Oui`/`Non`, oui par défaut). Une assiette de resto qu'on ne remangera jamais n'a pas à encombrer la bibliothèque ; un plat qu'on refait toutes les semaines, si.
- **Ajout rapide en trois modes** — un bloc de kcal/macros qui n'est pas un item (juste de quoi équilibrer la journée), un ingrédient qui en fabrique un, et le même avec son code-barres. Les trois partagent la même première section, au même endroit : changer de mode ajoute ou retire une section en dessous, ça ne redispose rien.
- **Vues de Food** — trois sous-onglets sur la même période : les quatre macros en cartes de graphe (les mêmes que l'onglet Vues, engrenage compris), la **répartition** jour par jour (une barre de hauteur constante découpée en % de calories P/G/L — c'est le partage des couleurs qui change, pas la hauteur), et les **micronutriments**, une carte par micro avec un filtre à pastilles (par défaut ceux qui ont de la matière sur la période ; les autres restent visibles mais éteints — savoir qu'un micro n'est renseigné nulle part est une information).
- **Un graphe de Food est un tracker** — son engrenage ouvre `TrackerModal`, la même page que pour un tracker, réduite à ce qui a un sens : nom, courbe, granularité, cumul, couleur. Ce qu'il suit vient de ce qu'on mange, pas d'un réglage.
- **Ligne d'objectif sur un graphe** — `ChartCard` accepte un `goalAt(ts)` optionnel et trace la consigne **en marches** : les objectifs sont datés, un trait droit d'un bout à l'autre dirait que la cible d'aujourd'hui valait déjà il y a deux mois. Elle entre dans le calcul de l'échelle.
- **Objectifs nutritionnels** journaliers avec barres de progression et alerte dépassement, **datés** : régler un objectif le pose à partir du jour qu'on regardait, les jours précédents gardent le leur. Dans les Vues, la ligne d'objectif est une marche qui suit celui de chaque jour, et « dans la cible » compare chaque jour au sien.
- **Portions d'un repas** — une recette déclare en combien de parts elle se coupe ; l'éditeur affiche ce qu'une portion vaut, et l'ajout à une journée demande combien on en a prises (une demie comprise). Le facteur pèse chaque ingrédient, donc le journal garde une ligne par ingrédient, chacune juste.
- **Sélectionner plusieurs lignes d'une journée** — un lien « Sélectionner » allume le mode ; la barre d'actions se pose sur la fenêtre, sous le pouce, parce que les lignes à cocher peuvent être n'importe où dans une page qui défile. Voir **Mode sélection** au lexique.
- **Un repas reste un ensemble dans la journée** — il se replie sous son nom (nombre de portions, nombre d'ingrédients, calories), se supprime d'un geste comme une ligne, et sa part se règle au bas du bloc : passer de 1 à 1,5 portion repèse chaque ingrédient. Pas de tabulation — décaler les lignes les sortirait de l'alignement des colonnes de macros, et il faudrait relire deux grilles au lieu d'une : c'est un filet vertical à gauche qui dit l'appartenance, et il tient à n'importe quelle profondeur.
- **Repas du jour en cartes** — les quatre moments de la journée sont quatre sections rectangulaires, comme le reste de l'app, plutôt que quatre titres qui coulent les uns dans les autres. Une ligne de journal **est** son bouton « modifier » : la taper ouvre sa fenêtre, et c'est là-dedans qu'on la supprime. La quantité y a sa propre pastille, détachée du nom — collée dessus, elle se lisait comme la fin du nom.
- **Sources en bulles** — le crédit reste écrit en une ligne (la licence d'Open Food Facts le demande) ; les paragraphes qui l'expliquent et la liste des liens passent derrière deux bulles « i ». Elles sont posées `always` : elles portent l'attribution, pas une explication de réglage, et ne suivent donc pas l'interrupteur des explications.
- **Retour immédiat après un enregistrement** — la page se referme sans attendre la base (voir « Écriture optimiste »), et un repas verse ses lignes en une seule insertion au lieu d'une par ingrédient.
- **Objectifs : la troisième macro se propose** — deux macros posées et une cible calorique ne laissent qu'une inconnue : le troisième champ l'affiche en placeholder, dans l'unité de SON mode (g, %, g/kg). Rien n'est enregistré tant qu'on n'a pas tapé — c'est une suggestion, pas une valeur.
- **Détail réglementaire + micronutriments** avec % des repères journaliers (AJR) quand disponibles.
- **PWA installable**, thème système, auth email/mot de passe + lien magique + réinitialisation.

## L'inférence se paie deux fois, et une seule est facturée

`analyse-repas` appelle Claude avec une clé API : chaque analyse coûte des
tokens, et c'est le chemin des *autres* utilisateurs. `tracklog-mcp` inverse la
flèche — ce n'est plus Tracklog qui appelle Claude, c'est Claude qui écrit dans
Tracklog depuis une conversation, donc sous un abonnement déjà payé. Les deux
coexistent volontairement : l'onglet IA reste la porte de l'app, le connecteur
est celle du propriétaire du compte. Il n'existe aucun moyen supporté d'utiliser
un abonnement comme identifiant d'API depuis une application — l'inversion est
la seule réponse, pas une astuce.
